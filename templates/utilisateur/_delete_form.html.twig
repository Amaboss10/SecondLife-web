<form method="post" action="{{ path('utilisateur_delete', {'id_personne': utilisateur.idPersonne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.idPersonne) }}">
    <button class="btn">Delete</button>
</form>
